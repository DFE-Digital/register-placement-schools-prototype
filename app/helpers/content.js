
const getSchoolTypeLabel = (code) => {
  return code
}

const getSchoolGroupLabel = (code) => {
  return code
}

const getSchoolStatusLabel = (code) => {
  return code
}

const getSchoolEducationPhaseLabel = (code) => {
  return code
}

const getAcademicYearLabel = (code) => {
  return code
}

module.exports = {
  getSchoolTypeLabel,
  getSchoolGroupLabel,
  getSchoolStatusLabel,
  getSchoolEducationPhaseLabel,
  getAcademicYearLabel
}
